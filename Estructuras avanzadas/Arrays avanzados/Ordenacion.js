


 // Ordenacion

let num = [1, 10, 3, 4, 7]

// Asendente

let orden = num.sort((a, b) => a - b)
console.log(orden)

// Desendente 
let orden2 = num.sort((a, b) => b - a)
console.log(orden2)

// De otra forma de desender es con el reverse

orden.reverse()
console.log(orden)

orden.reverse()
console.log(orden)